public abstract class ChaineCar {                   // Classe abstraite : peut etre heritee mais on ne peut pas creer d'instance de ChaineCar
    public abstract int len();                      // Fonction abstraite donc rien dans le corps

    public String toString() {                      // renvoie la chaine vide
        return "";  
    }
}